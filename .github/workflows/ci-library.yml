name: "CI for Go Kit using library"

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  ci-pipeline:
    uses: guillipa1993/devops-pipelines-ci-cd/.github/workflows/go-ci.yml@main
    with:
      go-version: "1.18"  # Versión de Go que deseas utilizar
